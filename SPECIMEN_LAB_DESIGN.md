# 바퀴 작업대 모드 설계서 v0.1

## 0. 목적

본편 1라운드에서 **족집게로 살아 있는 바퀴 한 마리를 집으면 독립된 정밀 작업대 모드로 전환**한다. 본편의 방·카메라·AI·미끼·시체 시스템은 일시정지하고, 작업대 안에서만 해부 파츠, 손상 상태, 도구 상호작용을 계산한다.

핵심 목표는 단순한 처치가 아니다.

- 한 마리의 외골격 구조가 실제로 여러 파츠와 관절로 존재한다.
- 완전 절단뿐 아니라 칼집, 긁힘, 함몰, 균열, 관통 실패, 부분 절단, 매달림, 분리 후 재절단이 존재한다.
- 고정하지 않으면 실험체가 작업대 안에서 도망가고, 도구에 끌리고, 몸을 비튼다.
- 고정하면 이동은 막히지만 각 다리와 몸통의 반사 운동은 더 크게 보인다.
- 실험체는 쉽게 종료되지 않는다. 큰 손상이 생겨도 `생존 → 쇠약 → 중추 손상 → 반사 운동만 남음`으로 단계가 나뉜다.
- 떨어진 조각도 사라지지 않고 계속 선택·이동·절단할 수 있다.

이 문서의 “생존”은 게임 상태다. 실제 생리 실험을 재현하려는 것이 아니라, 플레이 감각과 상태 지속성을 위한 추상화다.

---

## 1. 본편과의 결합 경계

본편과 연결되는 코드는 네 곳만 둔다.

1. 1라운드 도구 목록에 `족집게` 추가
2. 족집게 사용 시 가장 가까운 살아 있는 바퀴를 선택
3. `G.state = 'specimen'`으로 전환하고 본편 업데이트를 정지
4. 작업 종료 시 선택한 바퀴를 본편에서 제거하고 결과 등급만 반환

작업대는 별도 파일로 먼저 제작한다.

- `specimen.html`: 독립 실행 페이지
- `specimen-core.js`: 렌더러, 카메라, 입력, 오디오, 상태
- `specimen-anatomy.js`: 해부 모델과 파츠 그래프
- `specimen-tools.js`: 도구별 판정과 손상 처리
- `specimen-ui.js`: 버튼, 도움말, 상태 표시

본편 통합 때는 위 모듈을 그대로 가져오고 `specimen.html`의 부팅 코드만 본편 상태 전환 코드로 바꾼다.

---

## 2. 작업대 공간

### 2.1 구성

- 밝은 나무 작업대
- 흰색 반찬통 뚜껑 형태의 얕은 작업 접시
- 높이 2~3cm 정도의 투명 테두리
- 위쪽에서 비추는 작은 스탠드 조명
- 금속 트레이 한쪽에 도구 배치
- 바닥에 남는 작은 껍질·진물·그을음·핀 자국

작업대는 실험실이 아니라 **집에 있는 생활용품을 펼쳐 놓은 공간**으로 보이게 한다.

### 2.2 도망 제한

핀을 쓰지 않아도 실험체는 투명 테두리 안을 벗어나지 못한다.

- 멀쩡할 때는 벽을 따라 빠르게 움직인다.
- 한쪽 다리가 손상되면 원을 그리거나 방향이 흔들린다.
- 다리 수가 줄수록 이동량은 감소하지만 몸 비틀기와 남은 다리의 헛딛기는 유지된다.
- 몸통이 크게 손상된 뒤에는 전진보다 제자리 꿈틀거림 비율이 높아진다.

---

## 3. 해부 파츠 그래프

### 3.1 머리

- `head_capsule`: 머리 외피
- `eye_L`, `eye_R`: 좌우 겹눈 외피
- `antenna_L_0..11`, `antenna_R_0..11`: 더듬이 마디
- `mandible_L`, `mandible_R`: 좌우 큰턱
- `maxilla_L`, `maxilla_R`: 작은 먹이 조작부
- `maxillary_palp_L_0..4`, `maxillary_palp_R_0..4`
- `labial_palp_L_0..2`, `labial_palp_R_0..2`

### 3.2 가슴과 날개

- `pronotum`: 앞가슴등판
- `prothorax`, `mesothorax`, `metathorax`
- `tegmen_L`, `tegmen_R`: 앞날개/날개덮개
- `hindwing_L`, `hindwing_R`: 속날개

### 3.3 배

- `abdomen_01..10`
- 각 배마디는 등판과 배판을 하나의 상호작용 파츠로 시작하되, 깊은 커터 절개로 좌우 절반을 분리할 수 있다.
- `cercus_L_0..5`, `cercus_R_0..5`: 꼬리 감각기관 마디

### 3.4 다리 6개

각 다리는 다음 구조를 가진다.

- `coxa`
- `trochanter`
- `femur`
- `tibia`
- `tarsus_1..5`
- `claw_A`, `claw_B`

다리별 접두어:

- `LF`, `RF`: 앞다리
- `LM`, `RM`: 가운데다리
- `LH`, `RH`: 뒷다리

### 3.5 내부 노출용 보조 파츠

외골격이 열렸을 때만 보인다.

- `soft_tissue_head`
- `soft_tissue_thorax`
- `soft_tissue_abdomen_01..10`
- `ventral_cord_01..13`: 배쪽 신경삭 표현
- `trachea_branches`: 밝은 가는 관 모양 묶음

정밀한 의학 모델이 아니라 **외골격 파손의 깊이를 읽게 하는 시각적 층**이다.

---

## 4. 파츠 상태

각 파츠는 다음 상태를 가진다.

```js
{
  id,
  kind,
  parentId,
  children,
  attached: true,
  anchored: false,
  integrity: 1.0,
  scoreDepth: 0.0,
  crack: 0.0,
  puncture: 0.0,
  burn: 0.0,
  crush: 0.0,
  leak: 0.0,
  nerve: 1.0,
  twitch: 0.0,
  cutPoints: [],
  marks: [],
  fragmentLevel: 0
}
```

### 4.1 상태 단계

- `intact`: 멀쩡함
- `scored`: 표면에 선형 칼집
- `cracked`: 외피가 갈라짐
- `opened`: 외피가 벌어져 내부가 보임
- `partially_severed`: 일부 연결만 남고 매달림
- `detached`: 본체에서 분리
- `fragmented`: 분리된 조각이 다시 잘림
- `charred`: 열로 굳고 어두워짐

한 번의 입력으로 무조건 다음 단계로 넘어가지 않는다. 도구, 위치, 각도, 반복 횟수에 따라 같은 자리에 얕은 흔적만 여러 번 남을 수 있다.

---

## 5. 전체 생존·반사 상태

```js
vitals = {
  alive: true,
  awareness: 1.0,
  thoraxCore: 1.0,
  abdomenCore: 1.0,
  headCore: 1.0,
  shock: 0.0,
  heat: 0.0,
  fatigue: 0.0,
  mobility: 1.0,
  reflex: 1.0
}
```

### 5.1 게임 규칙

- 다리·더듬이·날개·한쪽 눈·한쪽 턱의 손상은 즉시 종료 조건이 아니다.
- 머리가 분리되어도 몸통 반사 운동은 일정 시간 또는 매우 긴 시간 유지한다.
- 이동 가능성은 남은 다리와 가슴 파츠 연결을 기반으로 계산한다.
- `alive`는 쉽게 꺼지지 않는다.
- 의도적으로 가슴 중심부와 배쪽 신경삭을 반복 파괴하거나, 전신을 장시간 가열한 경우에만 `alive=false`가 가능하다.
- `alive=false` 뒤에도 `reflex` 값이 남아 있으면 일부 다리와 배마디가 간헐적으로 움직인다.

### 5.2 움직임 합성

움직임은 세 층을 더한다.

1. **자발 이동**: 살아 있고 이동 가능한 다리가 있을 때
2. **도피 반응**: 도구 접근, 빛, 열, 전기 자극에 대한 급격한 몸 비틀기
3. **국소 반사**: 분리된 다리·더듬이·배마디의 짧고 불규칙한 떨림

손상이 심해질수록 애니메이션이 단순히 느려지는 것이 아니라 **비대칭·헛디딤·제자리 회전·갑작스러운 경련**으로 바뀐다.

---

## 6. 도구 설계

## 6.1 핀

목적: 몸통 또는 특정 파츠를 작업대에 고정한다.

조작:

- 파츠를 누르면 핀이 위에서 내려온다.
- 외피에 닿은 뒤 짧은 저항이 있고, 조금 더 누르면 고정된다.
- 같은 핀 머리를 다시 누르면 뽑힌다.
- 최대 6개.

효과:

- 핀 위치를 기준으로 해당 파츠의 이동이 제한된다.
- 한 군데만 고정하면 몸이 그 점을 중심으로 회전한다.
- 두 군데 이상 고정하면 본체 이동은 거의 막히고 팔다리만 크게 움직인다.
- 핀을 관절 바로 옆에 꽂으면 족집게 절단에 필요한 장력이 줄어든다.

## 6.2 족집게

목적: 잡기, 당기기, 뜯기, 조각 이동.

조작:

- 화면을 누르면 가장 가까운 집기 가능한 파츠에 자동 보정된다.
- 누른 채 이동하면 파츠가 따라온다.
- 본체가 고정되지 않았으면 실험체 전체가 끌려온다.
- 본체 또는 인접 파츠가 핀으로 고정되어 있으면 연결부에 장력이 쌓인다.
- 임계 장력을 넘으면 `partially_severed`를 거쳐 분리된다.

세부 결과:

- 더듬이 끝마디만 분리
- 한쪽 겹눈 외피만 분리
- 한쪽 큰턱만 분리
- 다리의 특정 마디 이하 전체 분리
- 이미 떨어진 조각을 옮기거나 뒤집기

## 6.3 손톱깎이

목적: 작은 관절과 돌출 부위를 짧고 정밀하게 절단.

조작:

- 터치 위치에서 가장 가까운 절단 후보가 강조된다.
- 누르고 있는 동안 입이 맞춰지고, 손을 떼면 레버가 닫힌다.
- 너무 두꺼운 가슴판이나 배 중앙은 한 번에 잘리지 않고 균열만 생긴다.

가능 대상:

- 더듬이 마디
- 부절·발톱
- 다리 관절 또는 다리 중간 절단점
- 큰턱·작은 입부속
- 겹눈 가장자리
- 날개 끝과 찢어진 가장자리

## 6.4 송곳

목적: 외골격을 눌러 깨뜨리고 관통점을 만든다.

조작:

- 누른 채 유지하면 끝이 표면을 누른다.
- 초반에는 외피가 함몰되고 도구가 미끄러질 수 있다.
- 충분히 오래 누르면 작은 균열이 먼저 생기고 갑자기 안쪽으로 들어간다.
- 일찍 떼면 구멍이 아니라 함몰·긁힘만 남는다.

결과:

- 눈에 커다란 원형 구멍을 뚫는 연출이 아니라, 작은 파손점·방사형 균열·내부 노출을 만든다.
- 같은 곳을 반복하면 파손점이 넓어진다.
- 얇은 날개는 쉽게 찢어지고, 가슴판은 가장 강한 저항을 가진다.

## 6.5 커터

목적: 자유로운 선형 절개와 반복 칼집.

조작:

- 누른 채 드래그하면 칼날이 표면을 따라간다.
- 화면에 보이는 선이 곧 절개 경로다.
- 한 번 긋는다고 자동으로 분리되지 않는다.
- 같은 경로를 반복하면 `scoreDepth`가 증가한다.

결과:

- 얕은 흠집
- 외피색이 갈라진 칼집
- 벌어진 절개
- 미리 정의된 구조 절단띠를 충분히 깊게 가로지르면 부분 절단
- 한 번 더 당기거나 자르면 완전 분리

커터는 쪽가위보다 자유도가 높으므로 1차 제작에서는 쪽가위를 제외한다. 쪽가위는 추후 “짧은 연속 절단” 전용 도구로 추가 가능하다.

## 6.6 목 긴 캔들 라이터

목적: 국소 가열, 날개 변형, 외피 변색, 움직임 변화.

조작:

- 누르고 있는 동안 끝에서 불꽃이 유지된다.
- 화면 이동으로 불꽃 위치를 옮긴다.

결과:

- 얇은 더듬이와 날개는 말리고 수축한다.
- 외피는 갈색 → 짙은 갈색 → 검게 변화한다.
- 열 자극 초반에는 도피·경련이 증가하고, 오래 가열한 파츠는 굳어 움직임이 감소한다.
- 전신 열 값이 매우 높아지기 전에는 즉시 종료되지 않는다.

## 6.7 압전 점화기

목적: 짧은 전기 펄스와 전신 경련.

형태:

- 가스라이터 안에 들어가는 압전 점화 모듈처럼 손가락으로 `딱` 누르는 장치
- 끝의 두 전극을 표면 가까이에 댄다.

조작:

- 한 번 누를 때마다 한 번의 짧은 스파크가 튄다.
- 누르고 있다고 연속 전류가 흐르지 않는다. 반복 클릭해야 한다.

결과:

- 접촉 지점에서 작은 섬광
- 전신 다리·더듬이의 순간 경련
- 분리된 다리도 가까우면 따로 튄다.
- 반복하면 작은 그을음과 신경 피로가 누적된다.
- 치명 도구가 아니라 반응을 만들어내는 도구로 시작한다.

---

## 7. 절단·파손 표현

### 7.1 칼집

- 3D 표면 바로 위에 짙은 선형 메시를 놓는다.
- 얕은 상태: 가늘고 어두운 선
- 중간 상태: 선 양쪽이 미세하게 벌어지고 밝은 내부색 노출
- 깊은 상태: 검은 가장자리 + 내부 조직 + 작은 방울

### 7.2 균열

- 송곳과 손톱깎이 실패 시 중심에서 2~5개의 짧은 균열선 생성
- 같은 지점 반복 시 기존 균열이 길어진다.

### 7.3 절단면

분리 시 양쪽에 각각 절단면을 만든다.

- 외곽: 어두운 키틴질 테두리
- 중간: 밝은 연조직
- 내부: 가는 관 또는 끈 모양 1~3개
- 족집게로 급하게 뜯으면 연결끈이 늘어난 뒤 끊어진다.
- 손톱깎이로 자르면 비교적 평평한 면이 남는다.

### 7.4 분리 조각

- 중력, 충돌, 미세 회전을 가진다.
- 작업 접시 안에서만 움직인다.
- 떨어진 뒤 일정 시간 국소 반사 운동.
- 핀으로 고정 가능.
- 족집게로 이동 가능.
- 커터와 손톱깎이로 다시 분할 가능.

---

## 8. 카메라와 조작

### 모바일

- 한 손가락: 현재 도구 사용
- 빈 공간 드래그: 작업 접시 회전
- 두 손가락: 카메라 이동/확대
- 도구 버튼은 화면 아래 가로 스크롤

### PC

- 왼쪽 버튼: 도구 사용
- 오른쪽 드래그: 카메라 회전
- 휠: 확대/축소
- 숫자키: 도구 변경

카메라가 너무 가까이 들어가도 모델 내부를 통과하지 않도록 최소 거리를 둔다.

---

## 9. 1차 제작 범위

이번 구현에서 반드시 작동해야 하는 것:

- 3D 작업대와 투명 작업 접시
- 해부 파츠가 분리된 작업대 전용 바퀴 모델
- 자발 이동·도피·경련·손상에 따른 비대칭 움직임
- 핀 6개까지 고정/제거
- 족집게로 파츠 잡기, 전체 끌림, 고정 후 장력 분리
- 손톱깎이로 작은 절단점 절단
- 송곳의 누름 → 함몰 → 균열 → 관통 단계
- 커터 드래그 칼집과 반복 깊이 증가
- 목 긴 라이터의 국소 가열
- 압전 점화기의 클릭별 전신 경련
- 눈, 턱, 더듬이, 다리 마디, 날개를 개별 선택
- 떨어진 파츠가 작업대에 남고 다시 선택 가능
- 상태 초기화

1차에서 보류:

- 본편 `index.html` 직접 결합
- 스크래퍼·수거통·압착기
- 쪽가위
- 완전 자유 메시 절단
- 유체 물리
- 내부 기관 전체 모델
- 저장/불러오기

---

## 10. 품질 판정 기준

1차 빌드는 아래 조건을 만족해야 “작동 모형”이 아니라 첫 플레이 버전으로 본다.

- 모델이 한 덩어리 그림이 아니라 눈·턱·더듬이·날개·다리 마디가 실제 깊이를 가진 3D 구조로 보인다.
- 핀이 없을 때 족집게로 당기면 몸 전체가 끌리고, 핀으로 고정한 뒤에는 부위가 뜯어진다.
- 커터를 한 번 그으면 선만 남고, 같은 곳을 반복해야 깊어진다.
- 송곳이 바로 통과하지 않고 저항과 실패 흔적이 보인다.
- 한쪽 다리를 잃었을 때 이동이 대칭적으로 느려지는 것이 아니라 자세와 진행 방향이 틀어진다.
- 겹눈·턱·다리 끝마디처럼 작은 부위를 모바일에서도 자동 보정으로 집을 수 있다.
- 떨어진 조각이 사라지지 않고 후속 조작이 된다.
- 라이터와 압전 점화기는 서로 다른 반응을 만든다.
- 대부분의 국소 손상으로는 즉시 종료되지 않는다.
